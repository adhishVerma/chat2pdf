import React from 'react'
import { Button } from '../ui/button'

const ChatSidebar = () => {
  return (
    <div>
        <Button disabled>Create new chat</Button>
    </div>
  )
}

export default ChatSidebar